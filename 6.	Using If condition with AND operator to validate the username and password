•	     Create a .sh file using editor 
•      Insert the following script into the file
#!/bin/bash

valid_username="Pranish"
valid_password="ok@123"

read -p "Enter your username: " username
read -sp "Enter your password: " password
echo

if [ "$username" == "$valid_username" ] && [ "$password" == "$valid_password" ]; then
    echo "Login Successful!"
else
    echo "Invalid username or password."
fi

 
•      Run the file using bash (file name) command
